name: "hello"
description: "A simple GitHub Action that prints a greeting message."
author: "Abiola"

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
